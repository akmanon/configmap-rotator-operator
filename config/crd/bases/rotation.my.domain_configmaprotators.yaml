---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: configmaprotators.rotation.my.domain
spec:
  group: rotation.my.domain
  names:
    kind: ConfigMapRotator
    listKind: ConfigMapRotatorList
    plural: configmaprotators
    singular: configmaprotator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ConfigMapRotator is the Schema for the configmaprotators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ConfigMapRotator
            properties:
              configMapName:
                description: ConfigMapName is the name of the ConfigMap to rotate
                type: string
              dataTemplate:
                additionalProperties:
                  type: string
                description: DataTemplate defines the structure of data to generate
                type: object
              rotationIntervalHours:
                description: RotationIntervalHours defines how often to rotate (in
                  hours)
                type: integer
            required:
            - configMapName
            - dataTemplate
            - rotationIntervalHours
            type: object
          status:
            description: status defines the observed state of ConfigMapRotator
            properties:
              currentGeneration:
                description: CurrentGeneration tracks the current version
                format: int64
                type: integer
              lastRotationTime:
                description: LastRotationTime tracks when the ConfigMap was last rotated
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
